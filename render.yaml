services:
  - type: web
    name: msn-messenger-server
    env: python
    buildCommand: pip install -r requirements-server.txt
    startCommand: python run_msn_server.py
    plan: free
    autoDeploy: true